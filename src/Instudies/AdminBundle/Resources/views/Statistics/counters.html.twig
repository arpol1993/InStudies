{% set title = 'Счетчики активности'%}

{% extends 'InstudiesAdminBundle:Statistics:statistics.html.twig' %}

{% block submenu_statistics_counters_attr %}class = "active"{% endblock %}

{% block content %}

    <div>
        <header class="page-header">
            <h2>{{ title }}</h2>                        
        </header>

        <div class = "row">
            <div class = "span5">
                <table class="table table-striped table-bordered table-condensed">                
                    <tread>
                        <tr>
                            <th>Параметр</th>
                            <th>Значение</th>
                        </tr>
                    </tread>
                    <tbody>
                    {% for param, value in table %}                    
                        <tr>
                            <td>{{param}}</td>
                            <td>{{value}}</td>
                        </tr>                   
                    {% endfor %}
                    </tbody>
                </table>
            </div>
        </div>
    </div>

{% endblock %}
